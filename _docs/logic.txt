0. Проверка, что если неправильный конфиг (опечатка), то ошибка
и выкидывание её в UI.
  - todo: separate func

1. Вычленить данные для отправки на фронт

2. Отправить данные, нужные для фронта, на фронт(

Сделать, чтобы json автоматически A-Z (с первого по четырнадцатый)

Спец: трай  try-catch в разных местах